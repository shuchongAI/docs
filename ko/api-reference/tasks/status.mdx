---
title: "작업 상태 조회"
description: "
- 비동기 작업의 실행 상태 및 결과 조회

- 실시간 상태 업데이트 및 진행률 추적

- 작업 완료 시 생성 결과 검색

- 다국어 지원（zh/en/ko/ja）
"
api: "GET https://api.apimart.ai/v1/tasks/{task_id}"

---





<RequestExample>

```bash cURL
curl --request GET \
  --url https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko \
  --header 'Authorization: Bearer <token>'
```

```python Python
import requests

url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt"

headers = {
    "Authorization": "Bearer <token>"
}

params = {
    "language": "ko"
}

response = requests.get(url, headers=headers, params=params)

print(response.json())
```

```javascript JavaScript
const url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko";

const headers = {
  "Authorization": "Bearer <token>"
};

fetch(url, {
  method: "GET",
  headers: headers
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error('Error:', error));
```

```go Go
package main

import (
    "fmt"
    "io/ioutil"
    "net/http"
)

func main() {
    url := "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko"

    req, _ := http.NewRequest("GET", url, nil)
    req.Header.Set("Authorization", "Bearer <token>")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

```java Java
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.net.URI;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko";

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(url))
            .header("Authorization", "Bearer <token>")
            .GET()
            .build();

        HttpResponse<String> response = client.send(request,
            HttpResponse.BodyHandlers.ofString());

        System.out.println(response.body());
    }
}
```

```php PHP
<?php

$url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko";

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    "Authorization: Bearer <token>"
]);

$response = curl_exec($ch);
curl_close($ch);

echo $response;
?>
```

```ruby Ruby
require 'net/http'
require 'json'
require 'uri'

url = URI("https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Get.new(url)
request["Authorization"] = "Bearer <token>"

response = http.request(request)
puts response.body
```

```swift Swift
import Foundation

let url = URL(string: "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko")!

var request = URLRequest(url: url)
request.httpMethod = "GET"
request.setValue("Bearer <token>", forHTTPHeaderField: "Authorization")

let task = URLSession.shared.dataTask(with: request) { data, response, error in
    if let error = error {
        print("Error: \(error)")
        return
    }
    
    if let data = data, let responseString = String(data: data, encoding: .utf8) {
        print(responseString)
    }
}

task.resume()
```

```csharp C#
using System;
using System.Net.Http;
using System.Threading.Tasks;

class Program
{
    static async Task Main(string[] args)
    {
        var url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko";

        using var client = new HttpClient();
        client.DefaultRequestHeaders.Add("Authorization", "Bearer <token>");

        var response = await client.GetAsync(url);
        var result = await response.Content.ReadAsStringAsync();

        Console.WriteLine(result);
    }
}
```

```c C
#include <stdio.h>
#include <curl/curl.h>

int main(void) {
    CURL *curl;
    CURLcode res;

    curl_global_init(CURL_GLOBAL_DEFAULT);
    curl = curl_easy_init();

    if(curl) {
        const char *url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko";

        struct curl_slist *headers = NULL;
        headers = curl_slist_append(headers, "Authorization: Bearer <token>");

        curl_easy_setopt(curl, CURLOPT_URL, url);
        curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headers);

        res = curl_easy_perform(curl);

        if(res != CURLE_OK) {
            fprintf(stderr, "curl_easy_perform() failed: %s\n",
                    curl_easy_strerror(res));
        }

        curl_slist_free_all(headers);
        curl_easy_cleanup(curl);
    }

    curl_global_cleanup();
    return 0;
}
```

```objectivec Objective-C
#import <Foundation/Foundation.h>

int main(int argc, const char * argv[]) {
    @autoreleasepool {
        NSURL *url = [NSURL URLWithString:@"https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko"];
        
        NSMutableURLRequest *request = [NSMutableURLRequest requestWithURL:url];
        [request setHTTPMethod:@"GET"];
        [request setValue:@"Bearer <token>" forHTTPHeaderField:@"Authorization"];
        
        NSURLSessionDataTask *task = [[NSURLSession sharedSession] 
            dataTaskWithRequest:request
            completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {
                if (error) {
                    NSLog(@"Error: %@", error);
                    return;
                }
                NSString *result = [[NSString alloc] initWithData:data 
                                                        encoding:NSUTF8StringEncoding];
                NSLog(@"%@", result);
            }];
        
        [task resume];
        [[NSRunLoop mainRunLoop] run];
    }
    return 0;
}
```

```ocaml OCaml
(* Requires cohttp and yojson libraries *)
open Lwt
open Cohttp
open Cohttp_lwt_unix

let url = "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko"

let () =
  let headers = Header.init ()
    |> fun h -> Header.add h "Authorization" "Bearer <token>"
  in
  
  let response = Client.get ~headers (Uri.of_string url) >>= fun (resp, body) ->
    body |> Cohttp_lwt.Body.to_string >|= fun body_str ->
    print_endline body_str
  in
  Lwt_main.run response
```

```dart Dart
import 'package:http/http.dart' as http;

void main() async {
  final url = Uri.parse('https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko');
  
  final response = await http.get(
    url,
    headers: {
      'Authorization': 'Bearer <token>',
    },
  );
  
  print(response.body);
}
```

```r R
library(httr)

url <- "https://api.apimart.ai/v1/tasks/task-unified-1757156493-imcg5zqt?language=ko"

response <- GET(
  url,
  add_headers(
    Authorization = "Bearer <token>"
  )
)

cat(content(response, "text"))
```

</RequestExample>

<ResponseExample>

```json 200 - 이미지 생성 작업
{
  "code": 200,
  "data": {
    "id": "task_01KA040M0HP1GJWBJYZMKX1XS1",
    "status": "completed",
    "progress": 100,
    "result": {
      "images": [
        {
          "url": [
            "https://upload.apimart.ai/f/image/9998236911693428-e8d7441f-f7b4-4130-97ad-9ef8a0dde2ce-image_task_01KA0413RT2GGNZJ9GWQ4PXF2F_0.png"
          ],
          "expires_at": 1763174708
        }
      ]
    },
    "created": 1763088289,
    "completed": 1763088308,
    "estimated_time": 60,
    "actual_time": 19
  }
}
```

```json 200 - 비디오 생성 작업 (썸네일 포함)
{
  "code": 200,
  "data": {
    "id": "task_01K9S419324DREZFBWNSVXYR6H",
    "status": "completed",
    "progress": 100,
    "result": {
      "thumbnail_url": "https://upload.apimart.ai/f/image/9998237146274576-da8a0eed-9229-4b31-97df-f3e9817d74c2-thumbnail_task_01K9S419324DREZFBWNSVXYR6H.jpg",
      "videos": [
        {
          "url": [
            "https://upload.apimart.ai/f/video/9998237146293438-170e0d9d-5d1d-421d-94fe-bcd0be7ae50a-video_task_01K9S419324DREZFBWNSVXYR6H.mp4"
          ],
          "expires_at": 1762940095
        }
      ]
    },
    "created": 1762853430,
    "completed": 1762853695,
    "estimated_time": 600,
    "actual_time": 265
  }
}
```

```json 400
{
  "error": {
    "code": 400,
    "message": "유효하지 않은 작업 ID입니다",
    "type": "invalid_request_error"
  }
}
```

```json 401
{
  "error": {
    "code": 401,
    "message": "인증 정보가 유효하지 않습니다",
    "type": "authentication_error"
  }
}
```

```json 402
{
  "error": {
    "code": 402,
    "message": "잔액이 부족합니다. 계정을 충전해 주세요",
    "type": "payment_required"
  }
}
```

```json 403
{
  "error": {
    "code": 403,
    "message": "접근이 금지되었습니다. 이 리소스에 대한 권한이 없습니다",
    "type": "permission_error"
  }
}
```

```json 429
{
  "error": {
    "code": 429,
    "message": "요청 한도를 초과했습니다. 나중에 다시 시도해 주세요",
    "type": "rate_limit_error"
  }
}
```

```json 500
{
  "error": {
    "code": 500,
    "message": "내부 서버 오류가 발생했습니다. 나중에 다시 시도해 주세요",
    "type": "server_error"
  }
}
```

```json 502
{
  "error": {
    "code": 502,
    "message": "잘못된 게이트웨이입니다. 서버를 일시적으로 사용할 수 없습니다",
    "type": "bad_gateway"
  }
}
```

</ResponseExample>

## 인증

<ParamField header="Authorization" type="string" required>
  모든 API 엔드포인트에는 Bearer Token 인증이 필요합니다

  API 키 가져오기:

  [API 키 관리 페이지](https://apimart.ai/console/token)를 방문하여 API 키를 가져오세요

  요청 헤더에 추가:

  ```
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## 경로 매개변수

<ParamField path="task_id" type="string" required>
  생성 API에서 반환된 작업 ID
</ParamField>

## 쿼리 매개변수

<ParamField query="language" type="string">
  응답 콘텐츠의 언어, 다음 값을 지원:
  - `zh` - 중국어
  - `en` - 영어
  - `ko` - 한국어
  - `ja` - 일본어
  
  기본값은 영어
</ParamField>

## 응답

<ResponseField name="id" type="string">
  고유한 작업 식별자
</ResponseField>

<ResponseField name="status" type="string">
  작업 상태 값:
  - `pending` - 처리 대기 중
  - `processing` - 진행 중
  - `completed` - 성공적으로 완료됨
  - `failed` - 실패
  - `cancelled` - 사용자에 의해 취소됨
</ResponseField>

<ResponseField name="progress" type="integer">
  작업 진행률 (0–100)
</ResponseField>

<ResponseField name="result" type="object">
  작업 결과, 상태가 `completed`일 때만 반환됩니다

  <Expandable title="속성">
    <ResponseField name="images" type="array">
      생성된 이미지 객체 배열 (이미지 생성 작업의 경우)
    </ResponseField>

    <ResponseField name="videos" type="array">
      생성된 비디오 객체 배열 (비디오 생성 작업의 경우)
    </ResponseField>

    <ResponseField name="thumbnail_url" type="string">
      비디오 썸네일 URL (비디오 생성 작업의 경우)
      
      <Note>
      **중요:** 이 필드는 작업 생성 시 요청 매개변수에서 썸네일 생성을 명시적으로 지정한 경우에만 반환됩니다. 썸네일 생성이 요청되지 않은 경우 이 필드는 응답에 포함되지 않습니다.
      </Note>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created" type="integer">
  작업 생성 타임스탬프
</ResponseField>

<ResponseField name="completed" type="integer">
  작업 완료 타임스탬프 (완료 시에만 존재)
</ResponseField>

<ResponseField name="estimated_time" type="integer">
  예상 완료 시간(초)
</ResponseField>

<ResponseField name="actual_time" type="integer">
  실제 완료 시간(초) (완료 시에만 존재)
</ResponseField>

<ResponseField name="error" type="object">
  오류 세부정보 (상태가 `failed`일 때만 존재)

  <Expandable title="속성">
    <ResponseField name="code" type="integer">
      오류 코드
    </ResponseField>

    <ResponseField name="message" type="string">
      오류 메시지
    </ResponseField>

    <ResponseField name="type" type="string">
      오류 유형
    </ResponseField>
  </Expandable>
</ResponseField>