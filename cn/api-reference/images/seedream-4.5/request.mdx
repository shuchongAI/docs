---
title: "请求参数"
description: "SeeDream-4.5 图像生成请求参数"
---

# 请求参数

## 请求示例

<RequestExample>

```bash
curl -X POST https://api.apimart.ai/v1/images/generations \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "doubao-seedance-4-5",
    "prompt": "星空下的古老城堡",
    "size": "1:1",
    "n": 1
  }'
```

</RequestExample>

## 参数说明

<ParamField query="model" type="string" required>
  模型名称，固定为 "doubao-seedance-4-5"
</ParamField>

<ParamField query="prompt" type="string" required>
  图像生成的文本描述
</ParamField>

<ParamField query="size" type="string">
  生成图像的尺寸，支持 "1:1", "1024x1536", "1536x1024", "2k", "4k"
</ParamField>

<ParamField query="n" type="integer">
  生成图像的数量，默认为 1
</ParamField>

<ParamField query="image_urls" type="array">
  参考图像URL列表，用于图生图模式
</ParamField>

