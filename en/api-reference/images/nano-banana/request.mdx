---
title: "Request Parameters"
description: "Gemini-2.5-Flash-Image-preview image generation request parameters"
---

# Request Parameters

## Request example

<RequestExample>

```bash
curl -X POST https://api.apimart.ai/v1/images/generations \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-2.5-flash-image-preview",
    "prompt": "A bamboo forest path under moonlight",
    "size": "1:1",
    "n": 1
  }'
```

</RequestExample>

## Parameters

<ParamField query="model" type="string" required>
  Model name, fixed as "gemini-2.5-flash-image-preview"
</ParamField>

<ParamField query="prompt" type="string" required>
  Text description for image generation
</ParamField>

<ParamField query="size" type="string">
  Image dimensions. Supported: "1:1", "1024x1536", "1536x1024"
</ParamField>

<ParamField query="n" type="integer">
  Number of images to generate. Default: 1
</ParamField>

