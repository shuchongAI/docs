---
title: "Request Parameters"
description: "GPT-4o image generation request parameters"
---

# Request Parameters

## Request example

<RequestExample>

```bash
curl -X POST https://apimart.ai/v1/images/generations \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o-image",
    "prompt": "A beautiful sunset with vibrant colors over the ocean",
    "size": "1024x1024",
    "n": 1,
    "image_urls": [
      "https://example.com/image1.png",
      "https://example.com/image2.png"
    ]
  }'
```

</RequestExample>

## Parameters

<ParamField query="model" type="string" required>
  Model name. Use "gpt-4o-image".
</ParamField>

<ParamField query="prompt" type="string" required>
  Text description for image generation.
</ParamField>

<ParamField query="size" type="string">
  Output size. Supported: "1024x1024", "1792x1024", "1024x1792".
</ParamField>

<ParamField query="n" type="integer">
  Number of images to generate. Default: 1.
</ParamField>

<ParamField query="image_urls" type="array">
  Reference image URLs for image-to-image mode.
</ParamField>

